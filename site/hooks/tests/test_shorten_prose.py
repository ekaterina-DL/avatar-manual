from shorten_prose import REPLACEMENTS, on_page_markdown

QUALITY_THRESHOLD_SECTION = (
    "- С 23.02.2026 — порог качества повышен до **85%**.\n\n"
    "Качество на этом этапе оценивается по **ханипотам**.\n"
    "`[Переписка в Telegram с заказчиком, 16.02.2026 и 19.03.2026]`\n\n"
    "Отдельно у заказчика есть внутренний норматив времени на задание (ориентир — около 270 секунд) и\n"
    "практика квалифицировать как «фрод» одновременное выполнение двух заданий одним асессором\n"
    "(28.05.2026). Это управленческий контроль качества, а не критерий разметки — знать о нём полезно\n"
    "для понимания, почему можно получить предупреждение/отключение даже при формально верной\n"
    "разметке. `[Почта. ОС по Заявкам 26-28, 28.05.2026]`\n\n"
    "### Как принимается решение о предупреждении асессору\n\n"
    "Команда несколько раз спрашивала заказчика, по какому принципу выносятся официальные\n"
    "предупреждения — в частности, достаточно ли одной найденной ошибки в проверенном ролике из\n"
    "партии, или решение опирается на больший объём данных, не всегда видимый самому асессору. Ответ\n"
    "заказчика (07.08.2026): предупреждение выносится не за единичную ошибку саму по себе, а по\n"
    "совокупности критериев — если асессор **игнорирует требования инструкции**, **повторяет одни и\n"
    "те же ошибки**, **не учитывает обратную связь** (в т.ч. не изучает свои ошибки в сервисе «Мои\n"
    "ответы») и **не показывает положительной динамики**. При этом заказчик может смотреть не только\n"
    "задания, ушедшие на валидацию, но и **другие задания асессора**, не попавшие в выборку проверки —\n"
    "поэтому в системе может быть не видно всех данных, на основании которых принято решение.\n"
    "Официального процента выборки от объёма партии заказчик не назвал. `[Разметка ВК видео — ОС\n"
    "07.08.2026]`\n"
)

EXAM_SECTION = (
    "## Обучение и входной экзамен «Ступень 2»\n\n"
    "**Формат экзамена:** на каждый кейс даётся **15 минут таймера**. На обучающем видео перед\n"
    "экзаменом рекомендуется отсматривать примеры на скорости x1 (не ускоряя x1.5/x2 как для общей\n"
    "демонстрации) — на реальном экзамене торопиться не стоит, важнее не пропустить склейку или\n"
    "смену кадра. `[Транскрибация «Видео обучение проекта» — Аватар 2 этап]`\n"
    "Отдельно от таймера-лимита интерфейс экзамена показывает **среднее время на задание** (по\n"
    "наблюдению одного из потоков — ориентир около 8.5 минут на кейс) — это ориентир для темпа, а не\n"
    "жёсткий лимит (лимит — именно 15-минутный таймер выше). На второй попытке (в случае пересдачи)\n"
    "выдаются **другие видео**, не те же самые. `[Встреча с новичками по 2 этапу, 29.07.2026]`\n\n"
    "**Реальный темп работы (не норматив, а ориентир):** в начале проекта у команды выходило в\n"
    "среднем 10-15 размеченных кейсов в час; на 29.07.2026 куратор называл ориентир около 4 минут\n"
    "среднего времени на кейс в рабочем режиме (то есть заметно быстрее, чем на старте) — время сильно\n"
    "зависит от того, насколько сложное видео попалось (много склеек — можно потратить на поиск\n"
    "подходящих 10 секунд заметно больше времени). `[Встреча с новичками по 2 этапу, 29.07.2026]`\n\n"
    "**Нельзя пропустить кейс досрочно:** если видео/кейс попался сложный или просто хочется сделать\n"
    "паузу — предусмотренный вариант это выйти и сделать перерыв (15-30 минут) либо переключиться на\n"
    "другую заявку; сам кейс при этом никуда не денется — покинуть его раньше времени и взять другой\n"
    "нельзя, кейс освобождается для другого асессора только после того, как полностью истечёт время,\n"
    "отведённое на него. Это организационное правило платформы, а не критерий разметки.\n"
    "`[Встреча с новичками по 2 этапу, 29.07.2026]`\n\n"
    "Полный разбор ошибок экзамена — в начале [10-qa-log.md](10-qa-log.md).\n"
)


class FakeFile:
    def __init__(self, src_uri):
        self.src_uri = src_uri


class FakePage:
    def __init__(self, src_uri):
        self.file = FakeFile(src_uri)


class FakeSiteDir:
    def __init__(self, value):
        self._value = value

    def __str__(self):
        return self._value


class FakeConfig(dict):
    def __init__(self, site_dir):
        super().__init__()
        self.site_dir = FakeSiteDir(site_dir)


SITE_CONFIG = FakeConfig("/repo/avatar-manual-build/build")
PDF_CONFIG = FakeConfig("/repo/avatar-manual-build/build-pdf")


def test_untouched_on_pdf_build():
    page = FakePage("manual-2-etap/00-overview.md")
    result = on_page_markdown(QUALITY_THRESHOLD_SECTION, page, PDF_CONFIG, None)
    assert result == QUALITY_THRESHOLD_SECTION


def test_untouched_on_other_pages():
    page = FakePage("manual-3-etap/00-overview.md")
    result = on_page_markdown(QUALITY_THRESHOLD_SECTION, page, SITE_CONFIG, None)
    assert result == QUALITY_THRESHOLD_SECTION


def test_noop_if_source_text_already_changed():
    page = FakePage("manual-2-etap/00-overview.md")
    changed = "### Валидатор тоже может ошибаться\n\nСовсем другой текст.\n"
    result = on_page_markdown(changed, page, SITE_CONFIG, None)
    assert result == changed


def test_shortens_frod_norm_and_warning_decision_paragraphs():
    page = FakePage("manual-2-etap/00-overview.md")
    result = on_page_markdown(QUALITY_THRESHOLD_SECTION, page, SITE_CONFIG, None)
    assert "Команда несколько раз спрашивала заказчика" not in result
    assert "на основании которых принято решение." in result
    assert "точный процент выборки не разглашается" not in result
    assert "практика считать «фродом»" in result
    assert "**игнорирует\nтребования инструкции**" in result  # bold-критерии сохранены
    assert "- С 23.02.2026 — порог качества повышен до **85%**." in result  # соседний текст не тронут


def test_shortens_exam_format_and_removes_realistic_pace_paragraph():
    page = FakePage("manual-2-etap/00-overview.md")
    result = on_page_markdown(EXAM_SECTION, page, SITE_CONFIG, None)
    assert "Реальный темп работы" not in result
    assert "10-15 размеченных кейсов в час" not in result
    assert "**Формат экзамена:** на кейс даётся **15 минут таймера**" in result
    assert "**Нельзя пропустить кейс досрочно:** если кейс сложный" in result
    # заголовок раздела и хвост с ссылкой на журнал — не тронуты
    assert "## Обучение и входной экзамен «Ступень 2»" in result
    assert "Полный разбор ошибок экзамена — в начале [10-qa-log.md](10-qa-log.md)." in result
    # не остаётся двойного пустого абзаца на месте удалённого текста
    assert "\n\n\n" not in result


def test_segments_now_noop_after_consolidation_moved_into_source():
    """02-segments.md больше не в REPLACEMENTS — текст консолидации живёт прямо в файле
    (см. docs/superpowers/plans/2026-08-11-segments-dedup.md). Хук должен быть чистым no-op."""
    assert "manual-2-etap/02-segments.md" not in REPLACEMENTS
    page = FakePage("manual-2-etap/02-segments.md")
    src = "- Длительность одного сегмента: **от 10 до 300 секунд**.\n"
    result = on_page_markdown(src, page, SITE_CONFIG, None)
    assert result == src
