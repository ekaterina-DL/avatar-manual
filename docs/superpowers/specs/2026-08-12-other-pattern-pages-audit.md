# Аудит «другого паттерна подачи» (частые ошибки/FAQ/лайфхаки/спорные моменты/журнал ОС) [ЗАКРЫТО 12.08.2026]

Статус: закрыто без правок кода — аудит не нашёл механической работы, содержательный объём не
подлежит сокращению.

## Контекст

Пункт 2 из трёх, которые попросила сделать по очереди пользователь 12.08.2026 (вместе с пунктом
1 — перенос визуальной системы на 3 этап, см.
`2026-08-12-stage3-visual-system-design.md` — и пунктом 3 — «карточки-факты»). И `2026-08-07-site-content-restructure-design.md`,
и `2026-08-11-stage2-dedup-design.md` явно откладывали переупаковку страниц «другого паттерна
подачи» (частые ошибки/FAQ/лайфхаки/спорные моменты/журнал ОС) как «отдельный заход по желанию».
Этот документ — тот самый отдельный заход.

## Что проверено

Прочитаны целиком `manual-2-etap/07-faq.md`, `08-lifehacks.md`, `09-disputed-points.md` (аналоги
3 этапа — `05-faq.md`, `02-open-questions.md` — уже прочитаны при подготовке пункта 1).

## Вывод

**Механическая часть проблемы уже решена глобальными хуками, не относящимися к этим конкретным
файлам.** `embed_local_media.py` и `embed_video_links.py` встраивают плеер вместо любой голой или
markdown-ссылки на `.mp4`/vkvideo/youtube-shorts **на любой странице сайта**, а не только там,
где их специально включали (в отличие от `build_segment_examples.py`/`build_compare_cards.py`,
привязанных к конкретному файлу). Живых, ещё не встроенных голых ссылок на видео в этих файлах не
найдено.

**Компонент группировки в сетку (`group_media_lists.py`) сюда не подходит по форме контента, а не
из-за недоработки.** Он рассчитан на подряд идущие пункты списка (2+), где в каждом — ровно одно
видео. Реальная структура:
- `07-faq.md` — кейсы разделены заголовками `###` и горизонтальными линиями (`---`), не списком.
- `09-disputed-points.md` — ссылки на видео вкраплены внутрь абзацев аргументации по каждому
  спорному пункту, не идут отдельным списком.
- `08-lifehacks.md` — ссылок на видео нет вообще, только текстовые советы.

**Оставшийся объём — содержательная суть жанра, не декоративный дубль.** Диалоги «вопрос
оператора → ответ заказчика» (`07-faq.md`), цепочки датированных цитат по спорным вопросам
(`09-disputed-points.md`), пошаговые инструкции (`08-lifehacks.md`) — то, ради чего эти страницы
существуют. Сокращать/паковать в карточки без потери деталей источников здесь невозможно — та же
логика, по которой волна D (`2026-08-11-stage2-dedup-design.md`) не нашла реальных дублей в
`01`/`03`/`04`/`05`.

Единственная точечная находка — вопрос про объединение сегментов через склейку в `07-faq.md`
(раздел «Видео bYxShJqZid4») до сих пор помечен «⚠️ требует уточнения» в заголовке, но не имеет
парной статус-плашки, в отличие от всех пунктов `09-disputed-points.md` после волны 3. Это
несогласованность оформления, а не громоздкость — пользователь решила не тратить на это отдельную
правку, раз пункт 2 в целом закрывается без кода.

## Решение пользователя (12.08.2026)

Закрыть пункт 2 без правок кода и перейти к пункту 3 («карточки-факты»).
