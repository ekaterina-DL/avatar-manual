# Консолидация 02-segments.md (волна 1 дедупа 2 этапа) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Сделать `manual-2-etap/02-segments.md` каноническим источником для длины сегмента,
артефактов/склеек, порога молчания и зума — перенести в файл уже протестированную консолидацию
из `site/hooks/shorten_prose.py`, добавить фикс порога молчания (устраняет реальный рассинхрон с
`09-disputed-points.md`) и точечные тематические эмодзи, затем убрать ставшую мёртвой логику хука.

**Architecture:** Правки только в тексте `.md` (никаких новых хуков в этой волне). Текст для трёх
секций уже выведен и проверен — это результат прогона существующего, покрытого тестами
`shorten_prose.py` на реальном файле, с одним ручным фиксом (молчание) поверх. После переноса —
убрать из `shorten_prose.py` записи `REPLACEMENTS["manual-2-etap/02-segments.md"]` и
соответствующие тесты (текст в источнике и так будет совпадать с тем, что раньше подставлял хук
— подставлять станет нечего).

**Tech Stack:** MkDocs + pymdownx (сборка сайта), pytest (тесты хуков), git.

## Global Constraints

- Файлы вне `manual-2-etap/`, `manual-3-etap/`, `site/hooks/`, `site/theme/` не трогать (см.
  спецификацию `docs/superpowers/specs/2026-08-11-stage2-dedup-design.md`).
- **Не менять текст заголовков** (`#`/`##`) в `02-segments.md` — на них ссылаются другие файлы
  по якорю (`05-what-to-label.md#когда-видео-помечается-битое`,
  `07-faq.md#определение-и-границы`, `04-classifier.md#сколько-сегментов-выделять-на-одном-видео`
  дважды). Проверено: pymdownx-slugify меняет якорь даже при добавлении эмодзи к заголовку —
  эмодзи допустимы только внутри пунктов списка, не в самой строке заголовка.
- Эмодзи по темам (см. спецификацию): `⏱️ Длительность` · `🔇 Молчание` · `✂️ Склейки` ·
  `🚫 Битое`. Использовать только эти, не изобретать новые в этой волне.
- PDF-профиль (`is_pdf_build`) не трогаем — эта волна вообще не про хуки визуала, а про перенос
  уже существующей site-only консолидации в источник; после переноса PDF и сайт будут показывать
  одинаковый (новый) текст, что само по себе исправление (раньше PDF показывал более громоздкую
  версию, чем сайт).
- Каждое утверждение (цифры, источники в `` `[...]` ``) переносится 1:1 из уже одобренного вывода
  `shorten_prose.py` — см. Step 1-3, текст скопирован из реального прогона хука на текущем файле.

---

### Task 1: Консолидировать `02-segments.md` и убрать устаревшую логику `shorten_prose.py`

**Files:**
- Modify: `manual-2-etap/02-segments.md` (3 секции)
- Modify: `site/hooks/shorten_prose.py` (убрать ключ `"manual-2-etap/02-segments.md"` из `REPLACEMENTS`)
- Modify: `site/hooks/tests/test_shorten_prose.py` (убрать тесты и фикстуры для `02-segments.md`)

**Interfaces:** Нет — чисто контентная задача, публичных функций/сигнатур не меняется
(`on_page_markdown(markdown, page, config, files)` в `shorten_prose.py` остаётся как есть).

- [ ] **Step 1: Заменить секцию «Определение и границы»**

В `manual-2-etap/02-segments.md` найти блок, начинающийся с `- Длительность одного сегмента:` и
заканчивающийся строкой `` `[Инстр. Kandinsky-Аватар, стр.2, 10]` `` (между заголовками
`## Определение и границы` и `## Ограничения интерфейса`). Заменить целиком на:

```markdown
- ✂️ Сегмент не должен содержать артефакты: смену кадра, склейки, рамки, водяные знаки (ВЗ),
  наложенный текст и т.д. — и не должно быть смены сцены (человек не должен исчезать из кадра).
  ⚠️ Отдельный неоднозначный случай — склейки, **сглаженные нейросетью** (когда видео обработано
  ИИ так, что переход между кадрами выглядит плавным, без резкого скачка). Такие склейки в
  большинстве случаев не считаются ошибкой именно из-за отсутствия резкого перехода — но не все
  нейросети сглаживают одинаково хорошо: встречаются случаи, где обработка оставляет заметный
  скачок в доли секунды несмотря на попытку сгладить. Внимательно проверяйте такие моменты, а не
  пропускайте автоматически только потому, что переход выглядит «обработанным». `[Встреча с
  новичками по 2 этапу, 29.07.2026]`
  См. примеры настоящих склеек для сравнения — [06-common-mistakes.md](06-common-mistakes.md#4-сегмент-содержит-водяной-знак--наложенный-текст--склейку).
  `[Разметка ВК видео — ОС 07.08.2026]`
- ⏱️ **Длительность** сегмента — **от 10 до 300 секунд**; для этого в видео должна быть хотя бы
  **одна непрерывная сцена** (без смены ракурса, без склейки) с говорящим человеком
  длительностью более 10 сек — это минимальное условие, чтобы вообще можно было выделить
  сегмент. Если сегмент на вид «впритык» к 10 секундам — стоит замерить точнее, а не
  отбрасывать его сразу. `[Разметка ВК видео — ОС 07.08.2026]`
- 🔇 **Молчание** не должно превышать **2–3 секунды на границах сегмента** (в начале/конце) и
  **3–5 секунд внутри** сегмента — уточнённое практическое значение, дважды подтверждённое
  заказчиком напрямую и объединяющее более раннюю официальную инструкцию (3-4 сек) и памятку
  асессоров (2-3 сек); разбор источников — [09-disputed-points.md](09-disputed-points.md#допустимая-длительность-молчания-внутри-сегмента).
  Короткая пауза (1–2 сек) — **не повод** резать сегмент, как и короткий кашель, шмыганье носом,
  смешок «на вдохе» — это не молчание.
  `[Переписка с заказчиком по заявкам 26-28, 06.07.2026 и 17.07.2026]`
- **Где ставить границу:** сегмент должен начинаться с начала речи человека и заканчиваться в
  момент, когда он перестаёт говорить — не ставить границы «в произвольном месте», но не
  обязательно попадать ровно (например, ровно 300.000 сек — достаточно «на глаз», условно
  297 сек вместо 300). Не ставить границу **вплотную к склейке** или **затемнению экрана как
  переходу** (см. [05-what-to-label.md](05-what-to-label.md)) — нужно оставлять небольшой запас
  *до* них, не впритык (то же для минимальных ~10-сек сегментов рядом со склейкой: валидатор
  иногда кликает именно по границе, и клик на склейку засчитывается как ошибка, даже если
  сегмент формально ≥10 сек).
  `[Памятка «Аватар 2 этап», стр.1; Разметка ВК видео — ОС 19.05 СТ2; Транскрибация «Лайфхак
  для разметки» — Аватар 2 этап; Встреча с новичками по 2 этапу, 29.07.2026]`
- Перекрытие рта **на долю секунды** — допустимо, сегмент из-за этого обрезать не нужно. Но
  более длительное перекрытие (например, стаканом на 4 секунды) уже требует корректировки
  границ сегмента (исключить проблемный участок). `[ОС заказчика, 17.06.2026 — см. 10-qa-log.md]`
  Дополнительный количественный критерий: если рот перекрыт посторонним предметом/рукой/
  микрофоном **больше половины** времени сегмента — такой сегмент не подходит целиком, а не
  только в проблемной части. `[Разметка ВК видео — ОС 28.05 СТ2]`
  То же самое правило работает и для перекрытия **лица целиком** (не только рта): короткий
  эпизод перекрытия внутри длинного сегмента можно оставить как есть, но если участок с
  перекрытым лицом длится долго — сегмент нужно разделить на два (до и после), а не выделять
  одним куском. `[Разметка ВК видео — ОС 29.05 СТ2]`
  Отдельный нюанс про широкий микрофон (например, при профессиональной записи вокала в студии):
  если он закрывает часть щеки/подбородка, но **не сами губы** — это не считается перекрытием
  рта. Если артикуляция губ и мимика всё равно хорошо видны, такой сегмент берём в разметку.
  `[Видео с разбором ошибок — Аватар 2 этап]`
- **Резкий/быстрый зум — не берём** в сегмент (аналог тряски камеры). **Медленный/плавный зум —
  берём**: границы сегмента можно и нужно расширять на такие моменты, а не дробить сцену на
  несколько сегментов из-за них — можно выделить одним сегментом.
  `[Разметка ВК видео — ОС Экзамен 18.05 / 28.05 СТ2; Памятка «Аватар 2 этап», стр.4]`

`[Инстр. Kandinsky-Аватар, стр.2, 10]`
```

Проверка после правки: `grep -c "от 10 до 300 секунд" manual-2-etap/02-segments.md` внутри этой
секции должно давать 1 (не 2, как было раньше).

- [ ] **Step 2: Заменить секцию «Сколько сегментов выделять на одном видео»**

Найти блок от `## Сколько сегментов выделять на одном видео` до строки
`` `[Инстр. Kandinsky-Аватар, стр.2]` `` включительно (перед `## Когда видео помечается «Битое»`).
Не путать заголовок с телом — сам заголовок `## Сколько сегментов выделять на одном видео`
**остаётся без изменений** (на него ссылается `04-classifier.md`), просто под ним теперь один
короткий пункт, а всё остальное уезжает под новый заголовок `## Когда объединять/делить сегмент`.
Заменить весь блок на:

```markdown
## Сколько сегментов выделять на одном видео

- На одном видео может быть **несколько сегментов**, каждый со своим классификатором.

## Когда объединять/делить сегмент

- Если несколько подходящих фрагментов **однотипные** (не меняется объём и поза человека,
  ракурс и т.п.) — не нужно нарезать много одинаковых, достаточно **1-2 штуки** (по инструкции
  — 2, по памятке асессоров — 1-2, см. [09-disputed-points.md](09-disputed-points.md)). Если же
  участки, наоборот, **разные** по характеристикам (другой ракурс, другая эмоция и т.п.) — их
  нужно выделять отдельными сегментами, а не сваливать в один: правило «1-2 однотипных
  достаточно» не значит «всегда максимум 2 сегмента на видео» — оно только про **одинаковые**
  фрагменты. `[Разметка ВК видео — ОС 25.05 СТ2; Переписка в Telegram с заказчиком, 10.06.2026]`
- Если на видео есть длительная сцена, подходящая под критерии (от 10 до 300 секунд) — нужно
  выделить её **всю** целиком, а не дробить и не обрезать.
- Быстрая/интенсивная/непрерывная смена эмоций — один сегмент; если смена чёткая и долгая —
  делить на сегменты, но так, чтобы они не получались одинаковыми (официально подтверждено
  заказчиком, пункт 5 «Итоги встречи 18.05»). `[Памятка «Аватар 2 этап», стр.4; Переписка в
  Telegram с заказчиком, 18.05.2026]`
- Если в кадре человек долго статичен в одном плане (кадр не меняется) — не нужно дробить
  из-за случайных «перебивок», отмечаем один подходящий сегмент. `[Переписка в Telegram с
  заказчиком, 04.02.2026 — Итоги встречи]`
- «Динамичные» фрагменты (человек активно перемещается в кадре, приближается/удаляется от
  камеры) — нужно **активно выделять**, это важно для баланса датасета. Если ролик сначала
  долго статичен, а затем в движении — лучше разделить на два сегмента (статика / движение).
  Общее требование к лицу продолжает действовать: короткая потеря анфаса при повороте в
  длинном сегменте простительна, частые повороты или потеря лица в коротком — нет.
  `[Переписка в Telegram с заказчиком, 16.07.2026]`
- Если в ролике есть чётко различимые отдельные участки речи и пения — предпочтительно
  разделить на два сегмента, а не объединять и выбирать преобладающий тип (см.
  [04-classifier.md](04-classifier.md#уточнения-по-конкретным-полям-памятка-асессоров) про поле
  «Речь / Пение»). `[Переписка в Telegram с заказчиком, 30.07.2026]`

`[Инстр. Kandinsky-Аватар, стр.2]`
```

Известное следствие (не чинить в этой задаче — вне объёма волны 1): `04-classifier.md` дважды
ссылается на `02-segments.md#сколько-сегментов-выделять-на-одном-видео` по фактам, которые теперь
переехали под `## Когда объединять/делить сегмент` — переход по ссылке по-прежнему работает
(попадает на верную страницу и рядом), просто не на точный абзац. Уточнить якорь при правке
`04-classifier.md` в одной из следующих волн.

- [ ] **Step 3: Заменить открывающий блок секции «Когда видео помечается «Битое»»**

Найти блок от `## Когда видео помечается «Битое»` до конца абзаца, заканчивающегося на
`` `[Разметка ВК видео — ОС 01.06 СТ2]` `` (перед строкой `> Частая ошибка операторов...`).
Сам заголовок `## Когда видео помечается «Битое»` **остаётся без изменений** (на него ссылается
`05-what-to-label.md`). Заменить блок на:

```markdown
## Когда видео помечается «Битое»

🚫 Если на видео **нет ни одного** сегмента, подходящего под критерии — ставится галочка
«Битое» вместо выделения сегментов. `[Инстр. Kandinsky-Аватар, стр.1]` Перед отправкой в
«Битое» важно убедиться, что во всём видео действительно нет ни одного подходящего отрезка от
10 секунд — наличие одного «плохого» признака (например, где-то есть склейка) не значит, что
всё видео нужно браковать целиком. `[Памятка «Аватар 2 этап», стр.3]`

Это два взаимоисключающих варианта: **либо** выделяются сегменты, **либо** ставится «Битое» —
никогда не то и другое одновременно. `[ОС заказчика, 17.07.2026]`
```

Остальная часть секции (`> Частая ошибка операторов...` и всё, что после) не трогается.

- [ ] **Step 4: Убрать устаревшую запись в `shorten_prose.py`**

В `site/hooks/shorten_prose.py`, в словаре `REPLACEMENTS`, удалить целиком ключ
`"manual-2-etap/02-segments.md"` со всем его списком-значением — от строки
`    # 02-segments.md: по просьбе пользователя пересобираем...` (комментарий перед ключом) до
закрывающей `    ],` этого списка, включительно. После правки в `REPLACEMENTS` должен остаться
только один ключ — `"manual-2-etap/00-overview.md"`. Функция `on_page_markdown` не меняется —
`REPLACEMENTS.get(src_uri, [])` для `02-segments.md` вернёт пустой список, хук станет no-op для
этого файла (что и требуется — источник теперь и так в нужном виде).

- [ ] **Step 5: Переписать `test_shorten_prose.py` — убрать тесты и фикстуры для `02-segments.md`**

Файл `site/hooks/tests/test_shorten_prose.py` целиком заменить на:

```python
from pathlib import Path

from shorten_prose import on_page_markdown

VALIDATOR_SECTION = (
    "### Валидатор тоже может ошибаться\n\n"
    "Обратная связь (ОС) от валидатора — это не абсолютная истина. Каждую отмеченную в ОС ошибку\n"
    "стоит внимательно перечитывать и сверять с тем, что реально происходит на видео: если ваша\n"
    "разметка кажется вам явно верной, а комментарий валидатора — нет, **не соглашайтесь\n"
    "автоматически** — аргументированно переспрашивайте и, если нужно, оспаривайте (как именно —\n"
    "см. ниже). Заказчик рассматривает такие обращения и при необходимости поправляет валидатора, а\n"
    "не автоматически встаёт на его сторону. Подтверждённые реальные случаи: заказчик встал на\n"
    "сторону оператора против нового валидатора ([07-faq.md](07-faq.md#можно-ли-оспорить-решение-валидатора-да--и-заказчик-может-встать-на-сторону-оператора));\n"
    "на 3 этапе эталонный ханипот был задним числом скорректирован после обоснованного возражения\n"
    "исполнителя (см. [мануал 3 этапа](../manual-3-etap/05-faq.md)).\n\n"
    "### Как устроено оспаривание ошибки\n"
)

QUALITY_THRESHOLD_SECTION = (
    "- С 23.02.2026 — порог качества повышен до **85%**.\n\n"
    "Качество на этом этапе оценивается по **ханипотам**.\n"
    "`[Переписка в Telegram с заказчиком, 16.02.2026 и 19.03.2026]`\n\n"
    "Отдельно у заказчика есть внутренний норматив времени на задание (ориентир — около 270 секунд) и\n"
    "практика квалифицировать как «фрод» одновременное выполнение двух заданий одним асессором\n"
    "(28.05.2026). Это управленческий контроль качества, а не критерий разметки — знать о нём полезно\n"
    "для понимания, почему можно получить предупреждение/отключение даже при формально верной\n"
    "разметке. `[Почта. ОС по Заявкам 26-28, 28.05.2026]`\n\n"
    "### Как принимается решение о предупреждении асессору\n\n"
    "Команда несколько раз спрашивала заказчика, по какому принципу выносятся официальные\n"
    "предупреждения — в частности, достаточно ли одной найденной ошибки в проверенном ролике из\n"
    "партии, или решение опирается на больший объём данных, не всегда видимый самому асессору. Ответ\n"
    "заказчика (07.08.2026): предупреждение выносится не за единичную ошибку саму по себе, а по\n"
    "совокупности критериев — если асессор **игнорирует требования инструкции**, **повторяет одни и\n"
    "те же ошибки**, **не учитывает обратную связь** (в т.ч. не изучает свои ошибки в сервисе «Мои\n"
    "ответы») и **не показывает положительной динамики**. При этом заказчик может смотреть не только\n"
    "задания, ушедшие на валидацию, но и **другие задания асессора**, не попавшие в выборку проверки —\n"
    "поэтому в системе может быть не видно всех данных, на основании которых принято решение.\n"
    "Официального процента выборки от объёма партии заказчик не назвал. `[Разметка ВК видео — ОС\n"
    "07.08.2026]`\n"
)

EXAM_SECTION = (
    "## Обучение и входной экзамен «Ступень 2»\n\n"
    "**Формат экзамена:** на каждый кейс даётся **15 минут таймера**. На обучающем видео перед\n"
    "экзаменом рекомендуется отсматривать примеры на скорости x1 (не ускоряя x1.5/x2 как для общей\n"
    "демонстрации) — на реальном экзамене торопиться не стоит, важнее не пропустить склейку или\n"
    "смену кадра. `[Транскрибация «Видео обучение проекта» — Аватар 2 этап]`\n"
    "Отдельно от таймера-лимита интерфейс экзамена показывает **среднее время на задание** (по\n"
    "наблюдению одного из потоков — ориентир около 8.5 минут на кейс) — это ориентир для темпа, а не\n"
    "жёсткий лимит (лимит — именно 15-минутный таймер выше). На второй попытке (в случае пересдачи)\n"
    "выдаются **другие видео**, не те же самые. `[Встреча с новичками по 2 этапу, 29.07.2026]`\n\n"
    "**Реальный темп работы (не норматив, а ориентир):** в начале проекта у команды выходило в\n"
    "среднем 10-15 размеченных кейсов в час; на 29.07.2026 куратор называл ориентир около 4 минут\n"
    "среднего времени на кейс в рабочем режиме (то есть заметно быстрее, чем на старте) — время сильно\n"
    "зависит от того, насколько сложное видео попалось (много склеек — можно потратить на поиск\n"
    "подходящих 10 секунд заметно больше времени). `[Встреча с новичками по 2 этапу, 29.07.2026]`\n\n"
    "**Нельзя пропустить кейс досрочно:** если видео/кейс попался сложный или просто хочется сделать\n"
    "паузу — предусмотренный вариант это выйти и сделать перерыв (15-30 минут) либо переключиться на\n"
    "другую заявку; сам кейс при этом никуда не денется — покинуть его раньше времени и взять другой\n"
    "нельзя, кейс освобождается для другого асессора только после того, как полностью истечёт время,\n"
    "отведённое на него. Это организационное правило платформы, а не критерий разметки.\n"
    "`[Встреча с новичками по 2 этапу, 29.07.2026]`\n\n"
    "Полный разбор ошибок экзамена — в начале [10-qa-log.md](10-qa-log.md).\n"
)


class FakeFile:
    def __init__(self, src_uri):
        self.src_uri = src_uri


class FakePage:
    def __init__(self, src_uri):
        self.file = FakeFile(src_uri)


class FakeSiteDir:
    def __init__(self, value):
        self._value = value

    def __str__(self):
        return self._value


class FakeConfig(dict):
    def __init__(self, site_dir):
        super().__init__()
        self.site_dir = FakeSiteDir(site_dir)


SITE_CONFIG = FakeConfig("/repo/avatar-manual-build/build")
PDF_CONFIG = FakeConfig("/repo/avatar-manual-build/build-pdf")


def test_shortens_validator_section_and_drops_confirmed_cases():
    page = FakePage("manual-2-etap/00-overview.md")
    result = on_page_markdown(VALIDATOR_SECTION, page, SITE_CONFIG, None)
    assert "Подтверждённые реальные случаи" not in result
    assert "эталонный ханипот" not in result
    assert "может поправить валидатора — не всегда встаёт на его сторону." in result


def test_keeps_surrounding_headings():
    page = FakePage("manual-2-etap/00-overview.md")
    result = on_page_markdown(VALIDATOR_SECTION, page, SITE_CONFIG, None)
    assert "### Валидатор тоже может ошибаться" in result
    assert "### Как устроено оспаривание ошибки" in result


def test_untouched_on_pdf_build():
    page = FakePage("manual-2-etap/00-overview.md")
    result = on_page_markdown(VALIDATOR_SECTION, page, PDF_CONFIG, None)
    assert result == VALIDATOR_SECTION


def test_untouched_on_other_pages():
    page = FakePage("manual-3-etap/00-overview.md")
    result = on_page_markdown(VALIDATOR_SECTION, page, SITE_CONFIG, None)
    assert result == VALIDATOR_SECTION


def test_noop_if_source_text_already_changed():
    page = FakePage("manual-2-etap/00-overview.md")
    changed = "### Валидатор тоже может ошибаться\n\nСовсем другой текст.\n"
    result = on_page_markdown(changed, page, SITE_CONFIG, None)
    assert result == changed


def test_shortens_frod_norm_and_warning_decision_paragraphs():
    page = FakePage("manual-2-etap/00-overview.md")
    result = on_page_markdown(QUALITY_THRESHOLD_SECTION, page, SITE_CONFIG, None)
    assert "Команда несколько раз спрашивала заказчика" not in result
    assert "точный процент выборки не разглашается." in result
    assert "практика считать «фродом»" in result
    assert "**игнорирует\nтребования инструкции**" in result  # bold-критерии сохранены
    assert "- С 23.02.2026 — порог качества повышен до **85%**." in result  # соседний текст не тронут


def test_shortens_exam_format_and_removes_realistic_pace_paragraph():
    page = FakePage("manual-2-etap/00-overview.md")
    result = on_page_markdown(EXAM_SECTION, page, SITE_CONFIG, None)
    assert "Реальный темп работы" not in result
    assert "10-15 размеченных кейсов в час" not in result
    assert "**Формат экзамена:** на кейс даётся **15 минут таймера**" in result
    assert "**Нельзя пропустить кейс досрочно:** если кейс сложный" in result
    # заголовок раздела и хвост с ссылкой на журнал — не тронуты
    assert "## Обучение и входной экзамен «Ступень 2»" in result
    assert "Полный разбор ошибок экзамена — в начале [10-qa-log.md](10-qa-log.md)." in result
    # не остаётся двойного пустого абзаца на месте удалённого текста
    assert "\n\n\n" not in result


def test_segments_now_noop_after_consolidation_moved_into_source():
    """02-segments.md больше не в REPLACEMENTS — текст консолидации живёт прямо в файле
    (см. docs/superpowers/plans/2026-08-11-segments-dedup.md). Хук должен быть чистым no-op."""
    page = FakePage("manual-2-etap/02-segments.md")
    src = "- Длительность одного сегмента: **от 10 до 300 секунд**.\n"
    result = on_page_markdown(src, page, SITE_CONFIG, None)
    assert result == src
```

- [ ] **Step 6: Прогнать тесты хуков**

Run: `cd site && python -m pytest hooks/tests/ -q`
Expected: все тесты зелёные (было 114, после удаления 9 тестов `02-segments.md` и добавления 1
нового — станет 106).

- [ ] **Step 7: Сравнить сборку сайта до/после**

По образцу проверки из предыдущей сессии (см. `manual-2-etap/00-overview.md` в истории коммитов):

```bash
cd "<repo-root>"
git stash
cd site
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python -m mkdocs build -f mkdocs.yml -d /tmp/_baseline > /tmp/baseline.txt 2>&1
cd ..
git stash pop
cd site
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 python -m mkdocs build -f mkdocs.yml -d /tmp/_after > /tmp/after.txt 2>&1
diff /tmp/baseline.txt /tmp/after.txt
```

Expected: `diff` не показывает новых строк `WARNING`/`INFO ... contains a link ... does not
contain an anchor` — набор предупреждений тот же, что и до правки (или меньше, но не больше).

- [ ] **Step 8: Показать диф пользователю, затем закоммитить**

Показать `git diff manual-2-etap/02-segments.md site/hooks/shorten_prose.py
site/hooks/tests/test_shorten_prose.py` пользователю на подтверждение. После подтверждения:

```bash
git add manual-2-etap/02-segments.md site/hooks/shorten_prose.py site/hooks/tests/test_shorten_prose.py
git commit -m "Мануал 2 этапа: 02-segments.md — единый канонический текст по длине/молчанию/склейкам/зуму,
фикс порога молчания (2-3/3-5 сек вместо устаревшей вилки 3-4/2-3), убран мёртвый код shorten_prose.py

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec coverage:** покрывает пункт 1 из «Порядок реализации» спецификации (`02-segments.md` —
крупнейший канонический владелец + фикс молчания). Визуальные компоненты (градиентная шкала,
флоучарты, статус-плашки) намеренно вынесены за пределы этой волны — требуют отдельного
согласования CSS/hook-дизайна, который ещё не был утверждён с конкретными значениями (только
общая концепция), поэтому не могут быть расписаны без плейсхолдеров прямо сейчас. Ссылки-фиксы в
`01-general-requirements.md`, `05-what-to-label.md`, `04-classifier.md`, `09-disputed-points.md` —
следующие волны, per design doc.

**Placeholder scan:** чисто, весь текст — финальный, скопирован из проверенного вывода хука +
ручной фикс, ни одного TBD/TODO.

**Type consistency:** N/A (контентная задача, не код с интерфейсами между тасками).
